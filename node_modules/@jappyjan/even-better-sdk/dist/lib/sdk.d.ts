import { EvenAppBridge, EvenHubEvent } from '@evenrealities/even_hub_sdk';
import { EvenBetterPage } from './page.js';
import { EvenBetterLoggerImplementation, EvenBetterLogLevel } from './logger.js';
export type EventListener = (event: EvenHubEvent) => void;
export declare class EvenBetterSdk {
    private readonly pages;
    static logger: EvenBetterLoggerImplementation;
    private static get state();
    static get bridge(): EvenAppBridge | null;
    private static set bridge(value);
    private static get currentPageId();
    private static set currentPageId(value);
    private static get eventListeners();
    private static get renderRequests();
    private static get renderInProgress();
    private static set renderInProgress(value);
    private static get initializationPromise();
    private static set initializationPromise(value);
    static setLogger(logger: EvenBetterLoggerImplementation): void;
    static setLogLevel(level: EvenBetterLogLevel): void;
    createPage(identifier: string): EvenBetterPage;
    get isInitialized(): boolean;
    private static initialize;
    getValue(key: string): Promise<string>;
    setValue(key: string, value: string): Promise<void>;
    static getRawBridge(): Promise<EvenAppBridge>;
    renderPage(page: EvenBetterPage): Promise<void>;
    private static processRenderRequests;
    addEventListener(listener: EventListener): void;
    removeEventListener(listener: EventListener): void;
}
//# sourceMappingURL=sdk.d.ts.map