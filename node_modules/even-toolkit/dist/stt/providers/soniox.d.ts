import type { STTProvider, STTProviderConfig, STTMode, STTState, STTTranscript, STTError } from '../types';
export declare class SonioxProvider implements STTProvider {
    readonly type: "soniox";
    readonly supportedModes: STTMode[];
    private _state;
    private apiKey;
    private modelId;
    private sampleRate;
    private language;
    private ws;
    private suppressSocketEvents;
    private transcriptCbs;
    private stateCbs;
    private errorCbs;
    get state(): STTState;
    init(config: STTProviderConfig): Promise<void>;
    start(): void;
    /** Send audio data to the Soniox stream. Converts Float32 to Int16 PCM. */
    sendAudio(data: ArrayBuffer | Int16Array | Float32Array): void;
    stop(): void;
    abort(): void;
    dispose(): void;
    onTranscript(cb: (t: STTTranscript) => void): () => void;
    onStateChange(cb: (s: STTState) => void): () => void;
    onError(cb: (e: STTError) => void): () => void;
    private closeSocket;
    private setState;
    private emitTranscript;
    private emitError;
}
//# sourceMappingURL=soniox.d.ts.map