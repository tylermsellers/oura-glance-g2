import { type EvenAppBridge, type EvenHubEvent } from '@evenrealities/even_hub_sdk';
import type { PageMode, SplitLayout } from './types';
export interface ColumnConfig {
    x: number;
    w: number;
}
export declare class EvenHubBridge {
    private sdk;
    /** Raw EvenHub SDK bridge — exposed for STT audio control */
    rawBridge: EvenAppBridge | null;
    private _currentMode;
    private _pageReady;
    private textPage;
    private textContent;
    private columnPage;
    private columnElements;
    private splitPage;
    private splitHeader;
    private splitLeft;
    private splitRight;
    private lastSplitLayoutKey;
    private chartDummyPage;
    constructor(columns?: ColumnConfig[]);
    get pageReady(): boolean;
    get currentMode(): PageMode | null;
    /** Alias matching even-market naming convention */
    get currentLayout(): PageMode | null;
    private createPages;
    init(): Promise<void>;
    setupTextPage(): Promise<boolean>;
    showTextPage(content: string): Promise<void>;
    updateText(content: string): Promise<void>;
    showColumnPage(columns: string[]): Promise<void>;
    updateColumns(columns: string[]): Promise<void>;
    private resolveSplitLayout;
    /**
     * Show a split page with a header + N panes.
     * Backwards-compatible: (header, left, right, layout?) still works.
     * N-pane: (header, panes[], layout?) where layout.paneWidths defines widths.
     */
    showSplitPage(header: string, leftOrPanes: string | string[], rightOrLayout?: string | SplitLayout, layout?: SplitLayout): Promise<void>;
    updateSplitPage(header: string, leftOrPanes: string | string[], rightOrLayout?: string | SplitLayout, layout?: SplitLayout): Promise<void>;
    switchToWatchlist(colSym: string, colPrice: string, colPct: string): Promise<boolean>;
    updateWatchlist(colSym: string, colPrice: string, colPct: string): Promise<void>;
    switchToSettings(text: string): Promise<boolean>;
    updateSettings(text: string): Promise<void>;
    switchToHomeLayout(menuText: string, imageTiles?: {
        id: number;
        name: string;
        x: number;
        y: number;
        w: number;
        h: number;
    }[]): Promise<boolean>;
    showHomePage(menuText: string, imageTiles?: {
        id: number;
        name: string;
        x: number;
        y: number;
        w: number;
        h: number;
    }[]): Promise<void>;
    updateHomeText(content: string): Promise<void>;
    switchToChartLayout(topText: string): Promise<boolean>;
    showChartPage(topText: string): Promise<void>;
    updateChartText(content: string): Promise<void>;
    sendImage(containerID: number, containerName: string, pngBytes: Uint8Array): Promise<void>;
    imuEnable(): Promise<void>;
    imuDisable(): Promise<void>;
    showShutdownContainer(exitMode?: 0 | 1): Promise<boolean>;
    onEvent(handler: (event: EvenHubEvent) => void): void;
    setLocalStorage(key: string, value: string): Promise<boolean>;
    getLocalStorage(key: string): Promise<string>;
    dispose(): void;
}
//# sourceMappingURL=bridge.d.ts.map