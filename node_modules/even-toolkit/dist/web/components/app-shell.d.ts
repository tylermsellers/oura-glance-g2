import type { ReactNode } from 'react';
interface AppShellProps {
    header: ReactNode;
    footer?: ReactNode;
    children: ReactNode;
    className?: string;
}
/**
 * AppShell — fixed header + scrollable content + optional fixed footer.
 * Uses flexbox so it works regardless of ancestor overflow settings.
 */
declare function AppShell({ header, footer, children, className }: AppShellProps): import("react/jsx-runtime").JSX.Element;
export { AppShell };
export type { AppShellProps };
//# sourceMappingURL=app-shell.d.ts.map