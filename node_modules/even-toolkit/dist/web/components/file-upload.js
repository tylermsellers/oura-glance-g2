import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import * as React from 'react';
import { cn } from '../utils/cn';
import { IcEditUploadToCloud } from '../icons/svg-icons';
function FileUpload({ onFiles, accept, multiple = false, maxSize, label, className }) {
    const [isDragging, setIsDragging] = React.useState(false);
    const [files, setFiles] = React.useState([]);
    const inputRef = React.useRef(null);
    const handleFiles = (fileList) => {
        if (!fileList)
            return;
        const arr = Array.from(fileList).filter((f) => !maxSize || f.size <= maxSize);
        setFiles((prev) => multiple ? [...prev, ...arr] : arr);
        onFiles(arr);
    };
    const handleDrop = (e) => {
        e.preventDefault();
        setIsDragging(false);
        handleFiles(e.dataTransfer.files);
    };
    const removeFile = (index) => {
        setFiles((prev) => {
            const next = prev.filter((_, i) => i !== index);
            onFiles(next);
            return next;
        });
    };
    const formatSize = (bytes) => {
        if (bytes < 1024)
            return `${bytes} B`;
        if (bytes < 1024 * 1024)
            return `${(bytes / 1024).toFixed(1)} KB`;
        return `${(bytes / (1024 * 1024)).toFixed(1)} MB`;
    };
    return (_jsxs("div", { className: cn('', className), children: [_jsxs("div", { onDragOver: (e) => { e.preventDefault(); setIsDragging(true); }, onDragLeave: () => setIsDragging(false), onDrop: handleDrop, onClick: () => inputRef.current?.click(), className: cn('border-2 border-dashed rounded-[6px] p-6 text-center cursor-pointer transition-colors', isDragging ? 'border-accent bg-accent-alpha' : 'border-border hover:border-accent'), children: [_jsx(IcEditUploadToCloud, { className: "w-8 h-8 mx-auto mb-2 text-text-dim" }), _jsx("p", { className: "text-[15px] tracking-[-0.15px] text-text-dim", children: label ?? 'Drop files or tap to browse' }), maxSize && (_jsxs("p", { className: "text-[11px] tracking-[-0.11px] text-text-muted mt-1", children: ["Max ", formatSize(maxSize)] })), _jsx("input", { ref: inputRef, type: "file", accept: accept, multiple: multiple, onChange: (e) => handleFiles(e.target.files), className: "hidden" })] }), files.length > 0 && (_jsx("div", { className: "flex flex-wrap gap-2 mt-3", children: files.map((f, i) => (_jsxs("span", { className: "bg-surface-light px-3 py-1.5 rounded-[6px] text-[13px] tracking-[-0.13px] inline-flex items-center gap-2", children: [f.name, _jsx("span", { className: "text-text-muted", children: formatSize(f.size) }), _jsx("button", { type: "button", onClick: (e) => { e.stopPropagation(); removeFile(i); }, className: "text-text-dim hover:text-negative cursor-pointer bg-transparent border-none p-0 text-[11px]", children: "\u2715" })] }, i))) }))] }));
}
export { FileUpload };
//# sourceMappingURL=file-upload.js.map