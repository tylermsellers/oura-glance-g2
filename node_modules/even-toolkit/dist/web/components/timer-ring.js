import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
function defaultFormat(seconds) {
    const m = Math.floor(seconds / 60);
    const s = seconds % 60;
    return `${m}:${s.toString().padStart(2, '0')}`;
}
function TimerRing({ remaining, total, size = 160, strokeWidth = 6, formatFn = defaultFormat, className, }) {
    const radius = (size - strokeWidth) / 2;
    const circumference = 2 * Math.PI * radius;
    const progress = total > 0 ? remaining / total : 0;
    const offset = circumference * (1 - progress);
    return (_jsxs("div", { className: `relative inline-flex items-center justify-center ${className ?? ''}`, children: [_jsxs("svg", { width: size, height: size, className: "-rotate-90", children: [_jsx("circle", { cx: size / 2, cy: size / 2, r: radius, fill: "none", stroke: "currentColor", strokeWidth: strokeWidth, className: "text-surface-lighter" }), _jsx("circle", { cx: size / 2, cy: size / 2, r: radius, fill: "none", stroke: "currentColor", strokeWidth: strokeWidth, strokeLinecap: "round", strokeDasharray: circumference, strokeDashoffset: offset, className: "text-accent timer-ring-circle" })] }), _jsxs("div", { className: "absolute inset-0 flex flex-col items-center justify-center", children: [_jsx("span", { className: "text-3xl font-bold tabular-nums font-[var(--font-display)]", children: formatFn(remaining) }), total > 0 && (_jsx("span", { className: "text-xs text-text-muted mt-1", children: remaining > 0 ? 'remaining' : 'done!' }))] })] }));
}
export { TimerRing };
//# sourceMappingURL=timer-ring.js.map