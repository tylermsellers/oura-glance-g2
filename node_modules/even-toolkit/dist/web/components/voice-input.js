import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import * as React from 'react';
import { cn } from '../utils/cn';
function WaveformVisualizer({ active, barCount = 5, color, className }) {
    return (_jsxs("div", { className: cn('flex items-center gap-[3px] h-6', className), children: [Array.from({ length: barCount }).map((_, i) => (_jsx("div", { className: "w-[3px] rounded-full transition-all", style: {
                    backgroundColor: color ?? 'var(--color-accent)',
                    height: active ? undefined : '4px',
                    animation: active ? `waveform-bar 0.8s ease-in-out ${i * 0.1}s infinite alternate` : 'none',
                } }, i))), active && (_jsx("style", { children: `
          @keyframes waveform-bar {
            0% { height: 4px; }
            100% { height: 20px; }
          }
        ` }))] }));
}
function VoiceInput({ onTranscript, language = 'en-US', className }) {
    const [state, setState] = React.useState('idle');
    const [transcript, setTranscript] = React.useState('');
    const recognitionRef = React.useRef(null);
    const SpeechRecognition = typeof window !== 'undefined'
        ? window.SpeechRecognition || window.webkitSpeechRecognition
        : null;
    const startListening = () => {
        if (!SpeechRecognition)
            return;
        const recognition = new SpeechRecognition();
        recognition.lang = language;
        recognition.continuous = false;
        recognition.interimResults = true;
        recognition.onstart = () => setState('listening');
        recognition.onresult = (e) => {
            const text = Array.from(e.results)
                .map((r) => r[0].transcript)
                .join('');
            setTranscript(text);
        };
        recognition.onend = () => {
            setState('result');
            if (transcript)
                onTranscript(transcript);
        };
        recognition.onerror = () => setState('idle');
        recognitionRef.current = recognition;
        recognition.start();
    };
    const stopListening = () => {
        recognitionRef.current?.stop();
        setState('processing');
    };
    const reset = () => {
        setTranscript('');
        setState('idle');
    };
    if (!SpeechRecognition) {
        return (_jsx("div", { className: cn('text-[13px] tracking-[-0.13px] text-text-dim', className), children: "Voice input not supported in this browser." }));
    }
    return (_jsxs("div", { className: cn('flex flex-col items-center gap-3', className), children: [_jsx("button", { type: "button", onClick: state === 'listening' ? stopListening : state === 'result' ? reset : startListening, className: cn('w-14 h-14 rounded-full flex items-center justify-center cursor-pointer transition-all', state === 'listening' && 'bg-accent animate-pulse scale-110', state === 'idle' && 'bg-surface hover:bg-surface-light', state === 'processing' && 'bg-surface-lighter', state === 'result' && 'bg-surface hover:bg-surface-light'), children: state === 'processing' ? (_jsx("div", { className: "w-5 h-5 animate-spin rounded-full border-2 border-surface-lighter border-t-accent" })) : (_jsxs("svg", { viewBox: "0 0 24 24", className: cn('w-6 h-6', state === 'listening' ? 'text-text-highlight' : 'text-text'), children: [_jsx("rect", { x: 10, y: 3, width: 4, height: 2, fill: "currentColor" }), _jsx("rect", { x: 10, y: 5, width: 4, height: 2, fill: "currentColor" }), _jsx("rect", { x: 10, y: 7, width: 4, height: 2, fill: "currentColor" }), _jsx("rect", { x: 10, y: 9, width: 4, height: 2, fill: "currentColor" }), _jsx("rect", { x: 8, y: 9, width: 2, height: 2, fill: "currentColor" }), _jsx("rect", { x: 14, y: 9, width: 2, height: 2, fill: "currentColor" }), _jsx("rect", { x: 6, y: 7, width: 2, height: 2, fill: "currentColor" }), _jsx("rect", { x: 16, y: 7, width: 2, height: 2, fill: "currentColor" }), _jsx("rect", { x: 6, y: 11, width: 2, height: 2, fill: "currentColor" }), _jsx("rect", { x: 16, y: 11, width: 2, height: 2, fill: "currentColor" }), _jsx("rect", { x: 8, y: 13, width: 8, height: 2, fill: "currentColor" }), _jsx("rect", { x: 11, y: 15, width: 2, height: 2, fill: "currentColor" }), _jsx("rect", { x: 8, y: 17, width: 8, height: 2, fill: "currentColor" })] })) }), state === 'listening' && _jsx(WaveformVisualizer, { active: true }), _jsxs("span", { className: "text-[13px] tracking-[-0.13px] text-text-dim", children: [state === 'idle' && 'Tap to speak', state === 'listening' && 'Listening...', state === 'processing' && 'Processing...', state === 'result' && 'Tap to try again'] }), transcript && (_jsx("div", { className: "bg-surface rounded-[6px] p-3 text-[15px] tracking-[-0.15px] text-text w-full", children: transcript }))] }));
}
export { VoiceInput, WaveformVisualizer };
//# sourceMappingURL=voice-input.js.map