import { jsx as _jsx } from "react/jsx-runtime";
import { cva } from 'class-variance-authority';
import { cn } from '../utils/cn';
const skeletonVariants = cva('bg-surface-lighter animate-pulse', {
    variants: {
        rounded: {
            default: 'rounded-[6px]',
            full: 'rounded-full',
            none: 'rounded-none',
        },
    },
    defaultVariants: {
        rounded: 'default',
    },
});
function Skeleton({ width, height, rounded, className }) {
    return (_jsx("div", { className: cn(skeletonVariants({ rounded, className })), style: {
            width: typeof width === 'number' ? `${width}px` : width,
            height: typeof height === 'number' ? `${height}px` : (height ?? '16px'),
        } }));
}
export { Skeleton, skeletonVariants };
//# sourceMappingURL=skeleton.js.map