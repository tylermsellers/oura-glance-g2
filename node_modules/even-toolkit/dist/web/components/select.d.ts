interface SelectOption {
    value: string;
    label: string;
}
interface SelectProps {
    value?: string;
    options: SelectOption[];
    onValueChange?: (value: string) => void;
    placeholder?: string;
    className?: string;
    disabled?: boolean;
    dropdownPosition?: 'top' | 'bottom';
    /** Invalid state. `true` shows a red ring; a string also renders the message below. */
    error?: boolean | string;
}
/**
 * Custom dropdown — no native <select>. Fully styled, no browser arrow issues.
 */
declare function Select({ value, options, onValueChange, placeholder, className, disabled, dropdownPosition, error }: SelectProps): import("react/jsx-runtime").JSX.Element;
declare namespace Select {
    var displayName: string;
}
export { Select };
export type { SelectProps, SelectOption };
//# sourceMappingURL=select.d.ts.map